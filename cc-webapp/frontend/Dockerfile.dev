# Casino-Club F2P Frontend Development Dockerfile

FROM node:18-alpine

# 개발 도구 설치
RUN apk add --no-cache git libc6-compat

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치 (개발 의존성 포함)
RUN npm install

# 소스 코드 복사 (개발 시에는 볼륨 마운트로 덮어씀)
COPY . .

# 개발 환경 변수 설정
ENV NODE_ENV development
ENV NEXT_TELEMETRY_DISABLED 1

# 포트 노출
EXPOSE 3000

# 개발 서버 시작 (핫 리로드 지원)
CMD ["npm", "run", "dev"]
